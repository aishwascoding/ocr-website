[phases.setup]
apt_packages = [
    "tesseract",
    "libtesseract-dev",
    "libleptonica-dev",
    "tesseract-ocr-eng",
    "tesseract-ocr-hin",
    "tesseract-ocr-fra",
    "tesseract-ocr-spa",
    "tesseract-ocr-chi-sim",
    "tesseract-ocr-ara",
    "tesseract-ocr-deu",
    "tesseract-ocr-jpn",
    "tesseract-ocr-kor",
    "tesseract-ocr-urd",
    "libgl1",
    "libsm6",
    "ffmpeg",
    "zlib1g",
    "imagemagick",
    "python3",
    "python3-pillow",
    "python3-flask",        # Install Flask via apt
    "python3-pip",          # Optional, for future use
    "gunicorn"              # ✅ Install Gunicorn system-wide
]

[phases.install]
cmds = [
    "mkdir -p /app/static/uploads"
]

[phases.build]
cmds = [
    "echo 'No build needed'"
]

[phases.start]
cmd = "gunicorn app:app --bind :$PORT"